title: Photos
icon: 📷

options:
  changeSlug: false
  changeStatus: false
  changeTemplate: false
  delete: false

sections:
  drafts:
    type: pages
    label: Drafts
    status: draft
    template: photo
    sortBy: date desc
    info: "{{ page.date.toDate('M d, Y H:i') }}"
    image:
      cover: true
  published:
    type: pages
    label: Published
    status: listed
    template: photo
    sortBy: date desc
    info: "{{ page.date.toDate('M d, Y H:i') }}"
    image:
      cover: true

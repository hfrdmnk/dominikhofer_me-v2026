title: Posts
icon: 📝

options:
  changeSlug: false
  changeStatus: false
  changeTemplate: false
  delete: false

sections:
  drafts:
    type: pages
    label: Drafts
    status: draft
    template: post
    sortBy: date desc
    info: "{{ page.date.toDate('M d, Y') }}"
  published:
    type: pages
    label: Published
    status: listed
    template: post
    sortBy: date desc
    info: "{{ page.date.toDate('M d, Y') }}"

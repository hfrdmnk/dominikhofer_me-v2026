title: Notes
icon: 💬

options:
  changeSlug: false
  changeStatus: false
  changeTemplate: false
  delete: false

columns:
  main:
    width: 2/3
    sections:
      drafts:
        type: pages
        label: Drafts
        status: draft
        template: note
        sortBy: date desc
        info: "{{ page.date.toDate('M d, Y H:i') }}"
      published:
        type: pages
        label: Published
        status: listed
        template: note
        sortBy: date desc
        info: "{{ page.date.toDate('M d, Y H:i') }}"

  sidebar:
    width: 1/3
    sections:
      bluesky:
        type: bluesky
        label: Bluesky Import
